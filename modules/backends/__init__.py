"""Shared multi-backend framework.

Lets a single detector run several implementations at once — a hand-rolled
heuristic plus one or more tested third-party models — and report each one's
result side by side (as heart_rate already does with classical vs open-rppg).
"""
